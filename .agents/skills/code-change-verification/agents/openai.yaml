interface:
  display_name: "Code Change Verification"
  short_description: "Run the required local verification stack"
  default_prompt: "Use $code-change-verification to run formatting, linting, mypy, pyright, and tests in the required order. Report any failures."
